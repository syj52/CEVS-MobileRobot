# cevs-server 前端开发指南

## 环境与运行

### 启动开发服务器
```bash
cd ESP/cevs-server
npm run dev
```
- 前端（Vite）: http://localhost:5173
- 后端 API（tsx watch）: http://localhost:8000
- TCP（ESP32 接入）: :5000

Vite 会把 `/api/*`、`/ws` 代理到 8000 端口（见 `vite.config.ts`），开发时前端端口 5173，WebSocket 自动拼接 `:8000`。

### Python 测试环境（torch258 conda）
本项目用 Playwright 做前端冒烟测试，安装在 `torch258` conda 环境中：

```bash
# 解释器路径
F:/conda/envs/torch258/python.exe

# 一次性安装（已完成）
F:/conda/envs/torch258/python.exe -m pip install playwright
F:/conda/envs/torch258/python.exe -m playwright install chromium
```

运行测试时用 webapp-testing 技能的辅助脚本管理 Vite 生命周期：
```bash
python "C:\Users\25703\.claude\skills\webapp-testing\scripts\with_server.py" \
  --server "npm run dev:front" --port 5173 --timeout 60 \
  -- F:/conda/envs/torch258/python.exe your_test.py
```

> 注意：当前 torch258 环境的 chromium 安装有已知问题，由用户手动判断反馈，AI 不再自动执行 chromium 相关测试。

---

## 前端文件结构
```
frontend/
├── index.html      # 面板骨架 + Tab 栏
├── main.ts         # 全部交互逻辑（Three.js / WebSocket / 各面板）
├── style.css       # 样式
├── logger.ts       # 日志组件
└── public/         # 静态资源（GLB 模型）
```

`main.ts` 是单文件入口，`<script type="module" src="/main.ts">` 由 Vite 直接加载 TS。

---

## 面板布局（当前）

### Tab 列表
| Tab | data-tab | 内容 |
|-----|----------|------|
| 🗺️ 导航地图 | `map` | **整合面板**：左侧 2D 地图 + 右侧侧边栏（标签/相机/位姿/调试） |
| 3D 场景 | `3d` | Three.js 空场景 |
| 手动控制 | `control` | 方向键、速度、原始帧 |
| 🔧 校准 | `calib` | 旋转/前进校准 |
| 📦 货物 | `goods` | 货物管理与取货 |
| 📹 视频 | `video` | MJPEG 流 + AprilTag 覆盖 |
| 仪表盘 | `dash` | 传感器数据卡片 |
| POI | `poi` | 兴趣点增删 |
| 日志 | `logs` | 系统日志 + LLM 对话 |

### 导航地图面板（核心整合面板）
左侧 `#map-main` 放 Canvas，右侧 `#map-sidebar`（280px）竖向堆叠四个分区：

```
#map-layout (flex row)
├── #map-main (flex:1)
│   ├── #map-canvas          ← 点击导航
│   ├── #map-info            ← 状态提示
│   └── #map-legend          ← 图例（absolute）
└── #map-sidebar (280px, overflow-y:auto)
    ├── 🏷️ 标签管理         ← tags-chips + 编辑表单
    ├── 📷 相机参数         ← fx/fy/cx/cy/tag_size
    ├── 📍 位姿设置         ← X/Y/角度 + 回原点
    └── 📈 实时调试         ← 位姿/里程计/标签融合/轨迹
```

> 之前的「📷 相机」「📈 调试」「🏷️ 标签」三个独立 Tab 已合并进此面板。

---

## 关键约定

### WebSocket 消息（main.ts `connectWs`）
| type | 方向 | 处理 |
|------|------|------|
| `state` | ← | 更新 robot 位姿、ESP 连接状态 |
| `stm32` | ← | 解析 `$SNSR/$ODOM/$IMU` 帧填仪表盘 |
| `apriltag` / `apriltag_status` | ← | 视频覆盖标记 |
| `voice_status` | ← | LLM 语音指令日志 |
| `calib_status` / `calib_result` | ← | 校准面板状态与结果 |
| `motion_trace` / `manual_move` | ← | 调试区轨迹与最近运动 |
| `motor` | → | 低延迟电机指令 |
| `subscribe_video` | → | 订阅 JPEG 流 |

### REST API（main.ts 各 `loadXxx`）
- 标签：`GET/POST /api/tags`、`DELETE /api/tags/:id`
- 相机：`GET/POST /api/camera`
- 位姿：`POST /api/robot/position`、`GET /api/debug`
- 校准：`POST /api/calib/{turn,drive,cancel}`、`GET /api/calib/status`
- 货物：`GET/POST /api/goods`、`POST /api/goods/pick`、`GET /api/goods/status`
- 导航：`POST /api/tag-nav/start`、`GET /api/tag-nav/status`、`POST /api/tag-nav/cancel`
- 坐标导航：`POST /api/nav`（直接）、`POST /api/nav/plan`+`POST /api/nav/execute`（规划→执行）、`POST /api/path-cancel`
- 紧急控制：`POST /api/stop`（紧急停止）、`POST /api/tag-nav/cancel`（取消导航）
- 巡逻：`POST /api/patrol/start`、`POST /api/patrol/arrive`
- 事件：`GET/POST /api/events`

### 未做 UI 的接口（低优先级 / 架构未用）
- `GET /api/ping` — 健康检查，无需 UI
- `GET /api/map`、`GET /api/map/data`、`POST /api/map/upload` — 服务端栅格地图。前端 `drawMap` 用本地 8×8 空栅格，未接服务端地图，上传功能暂未做（需先改造前端从服务端加载地图）
- `POST /api/nav-path`、`GET /api/path` — 与 `nav/plan` 重叠的旧路径接口
- WebSocket `video_stats` — 由 RTSP/ffmpeg 备用方案广播；当前视频走 ESP32 TCP→WS JPEG 流，前端自行计算 fps，不消费此消息

### 侧边栏轮询
进入 `map` Tab 时启动 `startDebugPolling()`（1s 轮询 `/api/debug`），切走时 `stopDebugPolling()`。标签/相机数据在进入 Tab 时加载一次，保存后刷新。

---

## 修改 Checklist
1. 新增元素 → 先在 `index.html` 加结构，`style.css` 加样式，`main.ts` 绑事件。
2. 新增 Tab → 在 `#tabs` 加 `<button class="tab" data-tab="xxx">`，加 `<div id="panel-xxx" class="panel">`，并在 Tab 切换逻辑里按需 `loadXxx()`。
3. 删除元素 → 同步清理 `main.ts` 中所有 `getElementById('...')` 引用，避免运行时报错。
4. 侧边栏分区用 `.sidebar-section` + `<hr class="sidebar-divider">` 分隔，表单用 `.form-grid-compact`。
5. ID 命名：调试数据 `dbg-*`、相机 `cam-*`、标签 `tag-*`、位姿 `pose-*`。

---

## 数据持久化文件

标签和相机参数各有 JSON 文件持久化，前端修改 → POST 后端 → 写盘。

### `config/camera.json` — 相机内参
```json
{ "fx": 1800, "fy": 1800, "cx": 640, "cy": 360, "tag_size_m": 0.168 }
```
- 读取方：`src/server/express.ts`（`GET /api/camera`）、`src/server/apriltagDetector.ts`（构造时读入，传给 Python 检测子进程）
- 写入方：`POST /api/camera`（`express.ts:153`）
- 默认值（文件缺失时）：`{fx:900, fy:900, cx:640, cy:360, tag_size_m:0.168}`
- 当前 `fx=fy=1800` 为实测校准值，请勿随意改回 900
- 前端「重置」按钮 = 重新从文件加载（`loadCameraConfig`），不覆盖文件

### `tags/tags.json` — AprilTag 世界坐标地图
```json
{ "<id>": { "x": <m>, "y": <m>, "z": <m>, "yaw": <rad>, "desc": "<中文描述>" } }
```
- 读取方：`src/server/tcp.ts`（启动时 `Object.assign(TAG_MAP, saved)`，文件覆盖代码默认值）
- 写入方：`setTagMapEntry` / `deleteTagMapEntry`（`tcp.ts:66` 的 `saveTags()`）
- 字段单位：x/y/z 米，yaw 弧度
- 默认布局（已填充）：4 个货架 + 原点
  - `0` 货架A-左上 (-0.3, 0.8) yaw=π
  - `1` 货架B-右上 (0.3, 0.8) yaw=π
  - `2` 货架C-左下 (-0.3, -0.8) yaw=0
  - `3` 货架D-右下 (0.3, -0.8) yaw=0
  - `9` 原点 (0, 0) yaw=0
- 注意：前端表单的 Yaw 输入是**度**，保存时 `* π/180` 转弧度；`loadTags` 显示时 `* 180/π` 转回度
- `tags/` 目录还存放 `dict_apriltag_36h11_id*.png`（id 0-10 的可打印标签图）

### 其它持久化
- `data/motion_records.jsonl` — 运动轨迹记录（`$T,...` 帧追加写入，`tcp.ts:59`）
- `maps/` — 导航栅格地图
